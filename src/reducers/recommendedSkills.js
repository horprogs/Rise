import ModelSkills from '../model/recommendedSkills';


const skills = (state = ModelSkills, action) => {
    switch (action.type) {

        default:
            return state;
    }
};

export default skills;